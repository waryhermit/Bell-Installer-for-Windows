{
   "_id": "_design/bell",
   "language": "javascript",
   "views": {
       "inbox": {
           "map": "function (doc) {\n      if (doc.receiverId) {\n        emit(doc.receiverId, true)\n      }\n    }"
       },
       "sentbox": {
           "map": "function (doc) {\n      if (doc.senderId) {\n        emit(doc.senderId, true)\n      }\n    }"
       },
       "unopened": {
           "map": "function (doc) {\n      if (doc.receiverId&&doc.status=='0' ) \n\t{\n        emit(doc.receiverId, 1)\n      }\n    }",
           "reduce": "function (keys,values)\n{\n\treturn sum(values)\n}"
       },
       "unopen": {
           "map": "function (doc) {\n      if (doc.receiverId&&doc.status=='0' ) \n\t{\n        emit(doc.receiverId, doc)\n      }\n    }"
       },
       "open": {
           "map": "function (doc) {\n      if (doc.receiverId&&doc.status=='1' ) \n\t{\n        emit(doc.receiverId, doc)\n      }\n    }"
       }
   }
}